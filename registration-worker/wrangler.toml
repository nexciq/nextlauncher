name = "nextlauncher-register"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets (set via: wrangler secret put GITHUB_TOKEN)
# GITHUB_TOKEN = fine-grained PAT, Contents: Read & Write on nexciq/nextlauncher
